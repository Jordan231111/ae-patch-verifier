{
  "name": "ae-patch-verifier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vercel dev",
    "check": "npm run check:syntax && npm run check:contracts",
    "check:syntax": "find api scripts -name '*.js' -type f -print0 | xargs -0 -n1 node --check",
    "check:contracts": "node scripts/check-modern-onceworld.js"
  }
}
